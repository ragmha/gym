appId: io.raghib.gym
name: Application Startup
onFlowStart:
  - clearState
  - launchApp

---
# Wait for the app to load
- waitForAnimationToEnd
# The dashboard is the root route; its first section header proves it rendered.
- assertVisible: 'Fitness Metrics' 